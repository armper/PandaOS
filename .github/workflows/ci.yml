name: CI

on:
  push:
    branches: [ main, develop, copilot/** ]
  pull_request:
    branches: [ main, develop ]

env:
  CARGO_TERM_COLOR: always

jobs:
  quality-gate:
    name: Quality Gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Install Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: nightly
          override: true
          components: rustfmt, clippy, rust-src, llvm-tools-preview
          target: x86_64-unknown-none
      
      - name: Cache cargo registry
        uses: actions/cache@v3
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
      
      - name: Cache cargo index
        uses: actions/cache@v3
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-cargo-index-${{ hashFiles('**/Cargo.lock') }}
      
      - name: Cache cargo build
        uses: actions/cache@v3
        with:
          path: target
          key: ${{ runner.os }}-cargo-build-target-${{ hashFiles('**/Cargo.lock') }}
      
      - name: Check formatting
        run: cargo fmt --all -- --check
      
      - name: Run clippy
        run: cargo clippy --workspace --target x86_64-unknown-linux-gnu --lib -- -D warnings
      
      - name: Run host unit tests
        run: cargo test --lib --workspace --target x86_64-unknown-linux-gnu
      
      - name: Build kernel
        run: cd kernel && cargo build --release

  # QEMU tests would go here once bootimage is working
  # qemu-test:
  #   name: QEMU Integration Tests
  #   runs-on: ubuntu-latest
  #   needs: quality-gate
  #   steps:
  #     - uses: actions/checkout@v3
  #     - name: Install QEMU
  #       run: sudo apt-get update && sudo apt-get install -y qemu-system-x86
  #     - name: Run QEMU tests
  #       run: ./scripts/qemu-test.sh
